{
  "name": "maidaid",
  "version": "0.1.0",
  "private": true,
  "description": "PWA for checking cleaner schedules, expenses, earnings and payments.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "preview:local": "npm run build && node scripts/local-preview.mjs",
    "test": "vitest run --exclude tests/e2e/**",
    "test:e2e": "npm run build && playwright test",
    "test:watch": "vitest --exclude tests/e2e/**"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.0.0",
    "@fastify/static": "^8.0.0",
    "dotenv": "^17.0.0",
    "fastify": "^5.0.0",
    "leaflet": "^1.9.4",
    "pg": "^8.16.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "playwright": "^1.62.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
