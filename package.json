{
  "name": "maidaid",
  "version": "0.1.0",
  "private": true,
  "description": "Personal WhatsApp assistant for cleaner schedules, actual hours, expenses and earnings.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "fastify": "^5.0.0",
    "fastify-raw-body": "^5.0.0",
    "googleapis": "^159.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
